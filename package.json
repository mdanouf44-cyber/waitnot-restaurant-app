{
  "name": "waitnot",
  "version": "1.0.0",
  "description": "Restaurant Discovery & Smart Ordering System with Reels UI",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "restaurant",
    "ordering",
    "reels",
    "pos"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.1.27"
  }
}
